services:
  experiment:
    build:
      context: .
      dockerfile: Dockerfile.cpu
    volumes:
      - ~/llm-models:/models
      - ~/llm-results:/results
      - ./data:/app/data
    environment:
      - RESULTS_DIR=/results
      - HF_HOME=/models
      - TRANSFORMERS_CACHE=/models
